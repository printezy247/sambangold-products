[pytest]
# `pytest` invoked as a console script does not put the working directory on
# sys.path — only `python -m pytest` does. Without this the test modules cannot
# import the app, which is exactly how CI failed on the first push.
pythonpath = .
testpaths = tests
